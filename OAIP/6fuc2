Необходимо выполнить расчет значений z1 и  z2 
по двум формулам с использованием функций func1 и
func2 соответственно. Эти функции должны возвращать
результат вычислений по формулам для конкретных исходных 
данных, передаваемых в функции в качестве параметров.
Исходные данные вводятся с клавиатуры. Результат расчета 
необходимо вывести на экран монитора. 
Программа должна позволять выполнять расчет по формулам
многократно для различных исходных данных, используя при этом следующую систему меню:
1. Ввод исходных данных с клавиатуры
2. Расчет по первой формуле
3. Расчет по второй формуле
4. Завершение работы программы
z1 = cos + sin+ cos3 + sin3									z2 = 2 cossin(/4 + 2)	

#include<locale>
#include <iostream>
#include <math.h>
#define PI 3.14159265358979323846

float func1(float a) {
	return cos(a) + sin(a) + cos(3 * a) + sin(3 * a);
}
float func2(float a) {
	return 2 * pow(2.0, 2)*cos(a)*sin(PI / 4 + 2 * a);

}
int main() {
	setlocale(LC_ALL, "Russian");
	int s = 0;
	float a;
	printf("\n1. Ввод исходных данных с клавиатуры\n2. Расчет по первой формуле\n3. Расчет по второй формуле\n4. Завершение работы программы\n");


	while (s != 4) {
		printf("Введите номер: ");
		scanf_s("%d", &s);

		if (s == 1) {
			printf("Введите данные:\n a= ");
			scanf_s("%f", &a);
		}
		else if (s == 2) {
			printf("Расчёт по 1 формуле:\nz1 = %d\n", func1(a));
		}
		else if (s == 3) {
			printf("Расчёт по 2 формуле:\nz2 = %d\n", func2(a));
		}
		else if (s == 4) {
			printf("Завершение программы.\n");
		}
	}

	return 0;
}
