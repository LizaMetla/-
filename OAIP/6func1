Необходимо вычислить и вывести на экран 20 значений математической функции
F на интервале от x = -5,0 до x = 5,0 с шагом 0,5. При этом фигурирующие 
в формулах коэффициенты a, b, c – это вещественные числа, которые предварительно 
вводятся с клавиатуры и, в отличие от x, не изменяются в процессе вычисления 
значений функции F. Подсчет одного значения функции F для конкретного значения
x необходимо реализовать с помощью функции Func, в которую передаются четыре 
параметра: x, a, b, c.
После получения результата в виде двадцати чисел программа должна вывести 
на экран следующий запрос: «Продолжить работу (1/0)?». При вводе с клавиатуры 
значения 1 описанный выше расчет должен повториться для новых значений коэффициентов 
a, b, c.  При вводе значения 0 – программа должна завершить свою работу.
 
#include <iostream>
#include <locale>
#include <stdio.h>
#include <stdlib.h>
float Func(float x, float a, float b, float c) {
	float f;

	if ((x + 5) < 0 && c == 0) {
		f = 1 / (a * x) - b;
	}
	else if ((x + 5) > 0 && c != 0) {
		f = (x - a) / x;
	}
	else {
		f = 10 * x / (c - 4);
	}
	return f;
}
int main() {
	setlocale(LC_ALL, "Russian");
	float a, b, c;
	int k = 1;
	while (k != 0) {
		printf("Введите a,b,c ");
		scanf_s("%f%f%f", &a, &b, &c);
	
		for (float x = -5; x <= 5; x += 0.5)
		{
			printf("\n%f %f\n", x, Func(x, a, b, c));

		}
		while (k != 0) {
			printf("\nПродолжить работу?\n1.да\n0.нет\n");
			scanf_s("%d", &k);
			break;
		}
	}
	return 0;
}

